# App
NODE_ENV=production
PORT=5000

# Database (Docker Compose)
# IMPORTANTE: Cambia estos valores para produccion
POSTGRES_USER=chatbot
POSTGRES_PASSWORD=change_me_secure_password
POSTGRES_DB=chatbot
DATABASE_URL=postgresql://chatbot:change_me_secure_password@db:5432/chatbot

# Auth (required in production)
# Genera secrets seguros con: openssl rand -hex 32
JWT_SECRET=change-me-generate-with-openssl-rand-hex-32
SESSION_SECRET=change-me-generate-with-openssl-rand-hex-32

# Optional AI integrations
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=
AI_INTEGRATIONS_GEMINI_API_KEY=
AI_INTEGRATIONS_GEMINI_BASE_URL=
OPENROUTER_API_KEY=

# Optional ElevenLabs
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# Optional Replit Auth (only if you deploy on Replit)
REPL_ID=
ISSUER_URL=
